apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-clusterip
spec:
  type: ClusterIP
  selector:
    app: {{.Release.Name}}
  ports:
    - name: http
      port: 8008
      targetPort: 8008